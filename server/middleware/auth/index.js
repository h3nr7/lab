const passport            = require('passport')

exports.init = () => {
	// passport.serializeUser(_serialize);
	// passport.deserializeUser(_deserialize);
	// passport.use('local', new localStrategy({
	// 		usernameField: 'email',
	// 		passwordField: 'password',
	// 		session: true
	// 	},
	// 	_strategyLocal
	// ));
  //
	// passport.use('hash', new hashStrategy(_strategyHash));
}
